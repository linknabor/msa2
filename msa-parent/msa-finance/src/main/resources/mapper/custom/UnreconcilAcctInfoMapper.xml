<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eshequ.msa.finance.mapper.custom.UnreconcilAcctInfoMapper">
<select id="getUnreconcilAcctInfo" resultType="hashmap">
   
   select * from msa_base_acct_info
   <where>
   		true
   		<if test="entity_name!=null">and entity_name LIKE CONCAT(CONCAT('%',#{entity_name}),'%')</if>
   		<if test="cust_name!=null">and cust_name LIKE CONCAT(CONCAT('%',#{cust_name}),'%')</if>
   		<if test="account_name!=null">and account_name LIKE CONCAT(CONCAT('%',#{account_name}),'%')</if>
   		<if test="account_no!=null">and account_no LIKE CONCAT(CONCAT('%',#{account_no}),'%')</if>
   		<if test="status!=null"> AND status = #{status}</if>
   </where>

</select>
</mapper>